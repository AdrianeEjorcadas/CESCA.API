import { NormalizeDatePipePipe } from './normalize-date-pipe-pipe';

describe('NormalizeDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new NormalizeDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
